const TaglineBlock = () => {
  return (
    <section className="section section-dark tagline-block" data-theme="dark">
      <div className="hfh-container narrow" style={{ textAlign: "center" }}>
        <h2 className="tagline-line">Feel frequencies instead of just hear them.</h2>
      </div>
    </section>
  );
};

export default TaglineBlock;
